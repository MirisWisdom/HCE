<Window
    x:Class="Promise.UI.Views.ConfigView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="HCE Configuration"
    Width="240"
    Height="420"
    Icon="/Resources/haloce_103.ico"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="180" />
            <RowDefinition Height="140" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <!--  Video Configuration  -->
        <GroupBox
            Grid.Row="0"
            Margin="5"
            Header="Video Configuration">
            <StackPanel Margin="5">
                <Label Content="Game Resolution:" />
                <DockPanel DockPanel.Dock="Top">
                    <Label Width="50" Content="Width:" />
                    <TextBox />
                </DockPanel>
                <DockPanel DockPanel.Dock="Top">
                    <Label Width="50" Content="Height:" />
                    <TextBox />
                </DockPanel>
                <Label Content="Display Adapter:" />
                <ComboBox>
                    <ComboBoxItem Content="Monitor 01" />
                    <ComboBoxItem Content="Monitor 02" />
                </ComboBox>
            </StackPanel>
        </GroupBox>

        <!--  Additional Toggles  -->
        <GroupBox
            Grid.Row="1"
            Margin="5"
            Header="Additional Toggles">
            <StackPanel Margin="5">
                <CheckBox Content="Console" />
                <CheckBox Content="Developer" />
                <CheckBox Content="Window Mode" />
                <CheckBox Content="Safe Mode" />
                <CheckBox Content="FF Mode" />
                <CheckBox Content="Screenshot" />
            </StackPanel>
        </GroupBox>

        <!--  Save button  -->
        <Grid Grid.Row="2" Margin="5">
            <Button Content="Save" />
        </Grid>
    </Grid>
</Window>
